export enum Status {
	ERROR = 'error',
	SUCCESS = 'success',
}

export enum ConnexionStatus {
	CONNECTED = 'connected',
	RECONNECTING = 'reconnecting',
	CONNECTING = 'connecting',
	DISCONNECTED = 'disconnected',
}
